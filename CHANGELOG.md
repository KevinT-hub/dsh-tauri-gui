# Changelog

本项目所有重要变更均记录于此。

## [v0.2.1]

### What's Changed
- 修复启动后等待 Web UI 就绪导致的空窗与卡顿问题，改为启动即预热缓存引擎并以桌面壳页面作为加载面貌，启动更快、更稳
- 修复前端状态模型依赖易变阶段文本的问题，统一改为基于稳定状态码（ShellStatusCode）的拉取式驱动，状态模型更健壮
- 修复事件监听在组件卸载时未统一注销的问题，所有订阅均显式 unlisten，避免页面因未订阅而卡死在某一界面、失去响应，并改善内存释放
- 修复冷启动或重连场景可能漏接更新通知的问题，改为前端挂载时主动拉取后端缓存的通知
- 调整更新浮层窗口高度，以适配更长的通知内容

> 本版本为小补丁迭代，主要修复已知缺陷，无新增功能。欢迎提交 issue 反馈问题。

## [v0.2.0]

### 重大重构：运行时不再内置，改为本机检测 + 按需安装

此前安装包内置 Node.js / npm / pnpm / dsh 等运行时；本次版本将其整体重写为「零内置运行时」模式：安装包本身不再打包任何运行时，转而在首次启动时检测你本机是否已具备所需依赖，缺失时可一键安排安装。

- **新增环境检测模块**（`src-tauri/src/detection/`）：对 Node、npm、pnpm、dsh 四项并行探测，逐项返回可执行路径、版本号与失败原因。
- **新增安装帮助与安排安装**（`detection/installer.rs` + `commands/setup.rs`）：检测发现缺失依赖时，给出官方安装动作——Node 跳转官方下载页、pnpm 通过 corepack 启用、dsh 执行官方包 `npm install -g @deepseek-ai/dsh`；所有安装动作均需在 UI 上显式确认后由壳层代为执行，绝不静默运行。
- **镜像策略**（`geo/`）：境内自动使用 npmmirror 镜像，境外回退官方源；geo 识别失败时安全回退官方源并允许手动切换。
- **首次启动门禁**：每个应用版本仅显示一次检测页，检测失败也不会反复打扰；托盘「重新检测环境」可随时手动触发。
- **官方引擎拉起**：以 `dsh web --no-open --port <port>` 启动，端口已被既有 `dsh web` 实例占用时优先连接而非重复拉起；引擎异常退出后自动重启。
- **桌面壳职责收敛**：壳层只负责环境检测、安装帮助、窗口生命周期、进程管理、自动更新与桌面体验，智能体/模型/工具/会话/Web UI 全部来自官方 `@deepseek-ai/dsh`。

### 文档与测试

- 同步更新 README、安装说明、使用指南与架构文档，统一「零内置运行时」表述。
- 配套补充跨层契约测试：`detection-contract.test.mjs`、`release-no-runtime.test.mjs` 等，覆盖检测与发行物策略。

> ⚠️ 破坏性变更：相较 v0.1.x，本版本不再随安装包内置运行时。升级后首次启动需按检测页指引自行准备 Node.js 22.19+/24+ 与官方 `@deepseek-ai/dsh`，或直接使用内置安装帮助完成安装。

## [v0.1.1]

### What's Changed
- 修复软件启动后自动拉起 dsh webui 的异常问题
- 修正发布者相关信息

> 本版本为小补丁迭代，主要修复已知缺陷，无新增功能。欢迎提交 issue 反馈问题。

## [v0.1.0]

项目首个可用版本，落地基础架构与核心功能。
功能完善中，存在较多优化空间，可供早期体验。
欢迎提交 issue 交流反馈。
